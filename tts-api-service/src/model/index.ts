/**
 * Domain models are provided by the Prisma client (`@prisma/client`): User,
 * TtsJob, JobEvent, IncidentAlert. Import Prisma types directly where needed;
 * this folder is reserved for hand-written domain aliases/mappers if required.
 */
export {};
